# Python
.venv/
venv/
__pycache__/
*.pyc

# Real scored dataset CSV — superseded by the committed Parquet (virs_final_scored_all.parquet,
# ~2 MB) which bakes into the Docker image. The large CSV stays local-only.
data/virs_final_scored_all.csv

# cluster_names.json IS committed — it's baked into the deployed image so road names work in prod.

# Local uvicorn log
_serve.log
